# Temporary workflow for testing release pipeline — DO NOT merge to main
name: Test release

on:
  push:
    branches: [chore/ci-release]

jobs:
  promote-images:
    uses: ./.github/workflows/release-promote-images.yml
    with:
      version: v0.1.0-rc.1
      short_sha: e7726f2
    permissions:
      contents: read
      packages: write

  promote-chart:
    uses: ./.github/workflows/release-promote-chart.yml
    with:
      chart_version: 0.1.0-rc.1
    permissions:
      contents: read
      packages: write

  # e2e:
  #   needs: [stage-images, stage-chart]
  #   uses: ./.github/workflows/release-e2e.yml
  #   with:
  #     version: v0.1.0-rc.1
  #     chart_version: 0.1.0-rc.1
  #   permissions:
  #     contents: read
  #     packages: read
