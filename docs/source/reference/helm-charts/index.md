# Helm Charts

Configuration reference for **Jupyter K8s** Helm charts.

| Chart | OCI URI | Description |
|-------|---------|-------------|
| [Operator](operator) | `oci://ghcr.io/jupyter-infra/charts/jupyter-k8s` | Controller, extension API server, CRDs, webhooks, RBAC |

```{toctree}
:hidden:

operator
```
