# Extension APIs

API reference for the **Extension API** server, serving the `connection.workspace.jupyter.org/v1alpha1` API group.

These APIs cannot be represented as CRDs — they are served by the Extension API server running in the controller pod.

```{toctree}
:hidden:

connection
bearer-token-review
connection-access-review
```
