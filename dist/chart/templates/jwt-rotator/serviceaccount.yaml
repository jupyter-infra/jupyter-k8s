{{- if and .Values.extensionApi.enable .Values.extensionApi.jwtSecret.enable }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: jwt-rotator
  labels:
    app: extensionapi-jwt-rotator
    component: security
{{- end }}
