{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.18.0
  name: workspaceaccessstrategies.workspaces.jupyter.org
spec:
  group: workspaces.jupyter.org
  names:
    kind: WorkspaceAccessStrategy
    listKind: WorkspaceAccessStrategyList
    plural: workspaceaccessstrategies
    singular: workspaceaccessstrategy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkspaceAccessStrategy is the Schema for the workspaceaccessstrategies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of WorkspaceAccessStrategy
            properties:
              accessResourceNamespace:
                description: |-
                  AccessResourcesNamespace is the namespace where routing resources will be created
                  If omitted, creates routes in the same namespace as the Workspace
                type: string
              accessResourceTemplates:
                description: AccessResourceTemplates defines templates for resources
                  created in the routes namespace
                items:
                  description: AccessResourceTemplate defines a template for creating
                    Kubernetes resources
                  properties:
                    apiVersion:
                      description: ApiVersion of the Kubernetes resource
                      type: string
                    kind:
                      description: Kind of the Kubernetes resource to create
                      type: string
                    namePrefix:
                      description: |-
                        NamePrefix is a prefix for the resource name
                        The name will be constructed as {NamePrefix}-{workspace.metadata.name}
                      type: string
                    template:
                      description: |-
                        Template is a YAML template string for the resource
                        Template variables include Workspace, AccessStrategy and Service objects
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - namePrefix
                  - template
                  type: object
                type: array
              accessURLTemplate:
                description: |-
                  AccessURLTemplate is a template string for constructing the workspace access URL
                  Template variables include .Workspace and .AccessStrategy objects
                  If not provided, the AccessURL will not be set in the workspace status
                  Example: "https://example.com/workspace-path/"
                type: string
              displayName:
                description: DisplayName is a human-readable name for this access
                  strategy
                type: string
              mergeEnv:
                description: |-
                  MergeEnv defines environment variables to be added to the main container
                  These will be merged with any existing env vars in the Workspace's container
                items:
                  description: AccessEnvTemplate defines a template for environment
                    variables
                  properties:
                    name:
                      description: Name of the environment variable
                      type: string
                    valueTemplate:
                      description: |-
                        ValueTemplate is a template string for the value
                        Can use variables from the Workspace or AccessStrategy objects
                        but not the Service object
                      type: string
                  required:
                  - name
                  - valueTemplate
                  type: object
                type: array
            required:
            - accessResourceTemplates
            - displayName
            type: object
          status:
            description: Status defines the observed state of WorkspaceAccessStrategy
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end -}}
