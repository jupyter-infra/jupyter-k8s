{{- if and .Values.extensionApi.enable .Values.extensionApi.jwtSecret.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app: extensionapi-jwt-rotator
    component: security
  name: {{ include "jupyter-k8s.resourceName" (dict "suffix" "jwt-secrets-reader" "context" $) }}
  namespace: {{ .Release.Namespace }}
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - list
  - watch
- apiGroups:
  - ""
  resourceNames:
  - {{ .Values.extensionApi.jwtSecret.secretName | quote }}
  resources:
  - secrets
  verbs:
  - get
{{- end }}
