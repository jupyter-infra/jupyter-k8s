# Example Workspace using Jupyter template with idle shutdown override
apiVersion: workspace.jupyter.org/v1alpha1
kind: Workspace
metadata:
  name: workspace-jupyter-override
  namespace: default
spec:
  displayName: "Jupyter Workspace with Idle Override"
  templateRef: "code-editor-template"
  desiredStatus: "Running"
  # Override idle shutdown with COMPLETE config (including detection)
  idleShutdown:
    enabled: true
    idleTimeoutInMinutes: 5  # 5 minutes (within 1-10 range)
    detection:
      httpGet:
        path: "/api/idle"
        port: 8888