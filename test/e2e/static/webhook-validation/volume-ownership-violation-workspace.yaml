apiVersion: workspace.jupyter.org/v1alpha1
kind: Workspace
metadata:
  name: volume-ownership-violation-test
spec:
  displayName: "Volume Ownership Violation Test"
  image: "jupyter/scipy-notebook:latest"
  desiredStatus: Running
  volumes:
  - name: stolen-volume
    persistentVolumeClaimName: workspace-owner-workspace-pvc
    mountPath: /stolen-data
