apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: rayclusters.ray.io
spec:
  group: ray.io
  names:
    kind: RayCluster
    listKind: RayClusterList
    plural: rayclusters
    singular: raycluster
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          # Minimal permissive schema sufficient for e2e resourceLookup tests.
          x-kubernetes-preserve-unknown-fields: true
      subresources:
        status: {}
