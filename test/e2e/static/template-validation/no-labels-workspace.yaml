apiVersion: workspace.jupyter.org/v1alpha1
kind: Workspace
metadata:
  name: no-labels-workspace
  namespace: default
spec:
  displayName: "Workspace with No Labels"
  templateRef:
    name: default-labels-template
    namespace: jupyter-k8s-shared
