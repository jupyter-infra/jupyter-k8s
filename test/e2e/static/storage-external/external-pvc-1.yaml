apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: external-pvc-1
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rancher-storage-class
