apiVersion: workspace.jupyter.org/v1alpha1
kind: WorkspaceTemplate
metadata:
  name: container-security-context-template
  namespace: jupyter-k8s-shared
spec:
  displayName: "Container Security Context Template"
  defaultImage: jk8s-application-jupyter-uv:latest
  defaultContainerSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
  resourceBounds:
    resources:
      cpu:
        min: 100m
        max: "2"
      memory:
        min: 128Mi
        max: 4Gi
